server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ms-user?createDatabaseIfNotExist=true
    username: root
    password: root123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    defer-datasource-initialization: false

  sql:
    init:
      mode: never

  rabbitmq:
    addresses: amqps://ndrtiiwr:dLDrX0QFV97MwObaUJAHRb71go8SNstt@gorilla.lmq.cloudamqp.com/ndrtiiwr

broker:
  queue:
    email:
      name: default.name